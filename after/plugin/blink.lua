print("[debug-agent] : blink.cmp is currently wip | chat r we cooked")

-- blink is a bit of an awkward one, to be hoenest with ya gng 
-- hopefully we fix ts bum ah RAHHHH


Blink = require("blink.cmp")

Blink.setup()
